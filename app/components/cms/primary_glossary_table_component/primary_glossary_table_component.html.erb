<%= render GovGridRowComponent.new(padding: {top: 0, bottom: 0}) do |row| %>
  <%= row.with_column("full") do %>
    <h2 class="govuk-heading-m"><%= @title %></h2>
    <table class="govuk-table primary-glossary-table-component govuk-!-margin-bottom-0">
      <thead class="govuk-table__head">
        <tr>
          <th class="govuk-table__header primary-glossary-table-component__header">Term</th>
          <th class="govuk-table__header primary-glossary-table-component__header">Key Stage</th>
          <th class="govuk-table__header primary-glossary-table-component__header">Definition</th>
        </tr>
      </thead>
      <tbody class="govuk-table__body">
        <% @records.each do |record| %>
          <tr class="govuk-table__row">
            <td class="govuk-table__cell"><%= record.term.value %></td>
            <td class="govuk-table__cell"><%= record.key_stage.value %></td>
            <td class="govuk-table__cell"><%= render Cms::RichTextBlockComponent.new(blocks: record.definition.blocks) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
<% end %>

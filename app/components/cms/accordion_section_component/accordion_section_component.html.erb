<%= render GovGridRowComponent.new(background_color: @background_color) do |row| %>
  <%= row.with_column("full") do %>
    <% if @title %>
      <h2 class="govuk-heading-m"><%= @title %></h2>
    <% end %>
    <div class="cms-accordion-section-component govuk-accordion govuk-body" data-module="govuk-accordion" id="accordion-default">
      <% @accordion_block.each_with_index do |block, index| %>
        <%= render block.render(index:)  %>
      <% end %>
    </div>
  <% end %>
<% end %>

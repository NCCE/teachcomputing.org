<%= content_tag :div, class: wrapper_classes do %>
  <div class="cms-resource-card__top-content">
    <div class="cms-resource-card__top-content-title">
      <h2 class="govuk-heading-m"><%= @title %></h1>
    </div>
    <div class="cms-resource-card__top-content-image">
      <%= render CmsImageComponent.new(@icon) if @icon %>
    </div>
  </div>
  <div class="cms-resource-card__body-text">
    <%= render CmsRichTextBlockComponent.new(blocks: @body_text, with_wrapper: false) %>
  </div>
  <%= link_to @button_text, @button_link, class: "govuk-button ncce-button--white" if @button_link %>
<% end %>

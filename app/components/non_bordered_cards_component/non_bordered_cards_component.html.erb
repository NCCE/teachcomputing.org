<%= content_tag :div, class: ['non-bordered-cards-component', { "#{@class_name}": @class_name.present? }] do %>
  <% @cards.each do | card | %>
    <%= render NonBorderedCardComponent.new(card) %>
  <% end %>
<% end %>

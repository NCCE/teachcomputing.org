<% meta_tag :url, request.original_url %>

<% @resource.class.resource_attribute_mappings.each do |mapping| %>
  <% view = @resource.resource_view(mapping) if mapping[:component] %>
  <% if view %>
    <%= render view%>
  <% end %>
<% end %>

<h3 class="govuk-heading-m">Your progress</h3>
<p class="govuk-body">A short paragraph about your activities being logged here. Soon we will launch credits feature that will help you track your progress towards certification. <a href="#" class="govuk-link">Read more about certification</a></p>

<ul class="govuk-list ncce-activity-list">
  <li class="ncce-activity-list__item">
    <span class="ncce-activity-list__item-text"><strong>Signed up</strong> to NCCE</span>
    <span class="ncce-activity-list__item-check-mark">&#9745;</span>
  </li>
  <% @achievements.each do |achievement| %>
    <li class="ncce-activity-list__item">
      <span class="ncce-activity-list__item-text"><strong><%= achievement.activity.title %></strong></span>
      <span class="ncce-activity-list__item-check-mark">&#9745;</span>
    </li>
  <% end %>
  <% @delegate_course_list.each do |course| %>
    <li class="ncce-activity-list__item">
      <span class="ncce-activity-list__item-text"><strong>Completed course</strong> <%= course.activity_title %></span>
      <span class="ncce-activity-list__item-check-mark">&#9745;</span>
    </li>
  <% end %>
  <li class="ncce-activity-list__item">
    <span class="ncce-activity-list__item-text"><strong>Add a CPD Activity</strong></span>
    <span class="govuk-form-group ncce-activity-form">
      <%= form_for Achievement.new, url: { controller: 'achievements', action: 'create' }, html: { class: 'ncce-activity-form"' } do |f| %>
        <%= f.label 'Choose activity', class: 'govuk-label ncce-activity-form__label' %>
        <%= f.select :activity_id,
          options_for_select(Activity.cpd.available_for(current_user).collect { |activity| [ activity.title, activity.id ] }),
          { include_blank: 'Select an activity' },
          { class: 'govuk-select ncce-activity-form__select' }
        %>
        <%= f.submit 'Add', class: 'govuk-button ncce-button__pink ncce-activity-form__button' %>
      <% end %>
    </span>
  </li>
</ul>
